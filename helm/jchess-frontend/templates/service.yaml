apiVersion: v1
kind: Service
metadata:
  name: {{ include "jchess-frontend.fullname" . }}
  labels:
    {{- include "jchess-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jchess-frontend.selectorLabels" . | nindent 4 }}
